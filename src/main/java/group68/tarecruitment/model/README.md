# Domain Models

Purpose:

- define core entities shared across the application

Suggested models:

- `TaProfile`
- `MoProfile`
- `AdminUser`
- `JobPosting`
- `ApplicationRecord`
- `SkillMatchResult`
- `ResumeInfo`

Important details:

- keep model fields aligned with the frontend field names where practical
- use enums for stable status values when possible

