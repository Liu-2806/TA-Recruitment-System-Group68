# Validation Module

Purpose:

- validate request fields before data is saved
- centralize reusable rules

Suggested validation targets:

- email format
- required fields
- password confirmation
- duplicate IDs
- duplicate email
- valid deadline date
- positive vacancy count

Suggested classes:

- `AuthValidator`
- `TaProfileValidator`
- `PostingValidator`
- `MoAccountValidator`

